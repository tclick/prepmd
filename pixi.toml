[project]
name = "prepmd"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.11"
pip = "*"

[tasks]
test = "python -m pytest"
lint = "ruff check ."
format = "ruff format ."
typecheck = "basedpyright"
